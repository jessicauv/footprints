# footprints
Footprints is a digital diary where you can document places you've been to. Built for the Yelp AI API Hackathon.

<!--## Setup

### Prerequisites
- Node.js (v18 or higher)
- npm or yarn
- Firebase account and project
- Yelp Fusion API key

### Installation

1. Clone the repository:
   ```bash
   git clone <repository-url>
   cd footprints
   ```

2. Install dependencies:
   ```bash
   npm install
   ```

3. Set up environment variables:

   **Option 1: System Environment Variable (Recommended)**
   ```bash
   # macOS/Linux
   export VITE_YELP_API_KEY=your_yelp_api_key_here

   # Windows
   set VITE_YELP_API_KEY=your_yelp_api_key_here
   ```

   **Option 2: .env file**
   ```bash
   cp .env.example .env
   # Edit .env and add your Yelp API key
   ```

   Get your Yelp API key from: https://www.yelp.com/developers/v3/manage_app

4. Set up Firebase:
   ```bash
   # Install Firebase CLI
   npm install -g firebase-tools

   # Login to Firebase
   firebase login

   # Use your Firebase project
   firebase use your-project-id

   # Deploy Firestore rules
   firebase deploy --only firestore:rules
   ```

5. Start the development server:
   ```bash
   npm run dev
   ```

## Features

- **Restaurant Journaling**: Search and select restaurants to journal about
- **AI-Generated Vibes**: Get 4-word personality descriptions for restaurants
- **Drag & Drop Editor**: Visual page editing with draggable elements
- **Persistent Storage**: All your journal entries are saved to Firebase
- **Location-Aware**: Uses your location for relevant restaurant suggestions-

## Tech Stack

- React + TypeScript
- Vite
- Firebase (Firestore + Auth)
- Yelp Fusion API
- Tailwind CSS (for styling)-->
